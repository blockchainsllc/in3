OPTION(IN3_SERVER "support proxy server" OFF)
OPTION(CMD "build the comandline utils" ON)
IF (CMD)
    IF (IN3_SERVER) 
       add_subdirectory(http-server)
    ENDIF (IN3_SERVER)
    add_subdirectory(in3)
    add_subdirectory(tools)
ENDIF (CMD)

